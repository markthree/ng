{
  "tasks": {
    "start": "deno run -A mod.ts",
    "release": "deno run -A --unstable https://deno.land/x/ndeno@v1.2.0/release.ts",
    "install": "deno install --allow-run --allow-read --allow-env --unstable -rfn g ./mod.ts"
  }
}
